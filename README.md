# APP-LOTEAMIENTO

MVP web para visualizar un loteamiento inmobiliario con React, Vite, TypeScript y Tailwind CSS.

## Desarrollo local

```bash
npm install
npm run dev -- --host
```

## Produccion

La app se publica en GitHub Pages con un workflow de GitHub Actions al pushear a `main`.
